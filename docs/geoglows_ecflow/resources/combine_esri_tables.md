geoglows_ecflow.resources.combine_esri_tables
=============================================

Functions
---------

The function ``combine_esri_tables`` is run as a task in the ecFlow job.


Running as a Script
-------------------

When running ``combine_esri_tables.py`` as a script, the following command line argument is available:

* **workspace** - Path to suite home directory

The following is an example of how `combine_esri_tables.py` could be run as a script:


    python combine_esri_tables.py /path/to/workspace
